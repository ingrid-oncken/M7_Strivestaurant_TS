//creating and exporting the export interface CommentsArray interface,
// CommentsArray is an array of objects
//this is the shape of the object containing the export interface CommentsArray {

export interface CommentsArray {
  id: number
  rating: number
  comment: string
  author: string
  date: string
}
